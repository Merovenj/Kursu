{
  "name": "@kursu/identity",
  "version": "0.0.1",
  "type": "module",
  "license": "AGPL-3.0-only",
  "main": "src/index.js",
  "scripts": { "test": "node --test", "lint": "echo ok", "build": "echo ok" },
  "comment": "Add @semaphore-protocol/identity, /group, /proof when KURSU_IDENTITY_BACKEND=semaphore"
}
